<h1>All Restaurants</h1>
<% @restaurants.each do |restaurant| %>
  <p><%= restaurant.title %>:   <%= restaurant.rating %></p>
<% end %>

<%= render partial: 'restaurants/form', locals: {user: @user} %>
